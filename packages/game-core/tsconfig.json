{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],

    "rootDir": "src",
    "outDir": "dist",
    "declaration": true,
    "declarationMap": true
  },
  // Tests are included here so they are typechecked and linted like everything
  // else. tsconfig.build.json is what excludes them from the published output.
  "include": ["src/**/*.ts"]
}
